---
description: Close or update a collection gap
argument-hint: close <G-id> [received|dropped] [reason]
step: 3
---

Update the gap $ARGUMENTS in `case.yaml` through mcp-case (gated):
mark it `received` (collecte recue) or `dropped` (renonce motivee), with the
analyst's reason. Gaps are never silently dropped. Journal the decision and
present the remaining gaps in French.
