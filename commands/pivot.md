---
description: Explore entities and relations around an entity
argument-hint: <entity value>
step: 4
---

Explore $ARGUMENTS through mcp-evidence `pivot` (entities/relations:
executed, created, deleted, connected_to, logged_on, persisted_via,
downloaded, spawned, modified). Present in French as a navigable summary:
the entity, its relations with timestamps and citations, cross-host
prevalence of behaviour tuples (not of binary names), suggested next hops.
